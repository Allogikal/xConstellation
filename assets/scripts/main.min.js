import{Starfield}from"./modules/starField.min.js";import{navigate,handleRouteChange}from"./modules/router.min.js";document.addEventListener("DOMContentLoaded",(()=>{document.body.addEventListener("selectstart",(e=>e.preventDefault()));document.body.addEventListener("contextmenu",(e=>e.preventDefault()));document.body.addEventListener("dragstart",(e=>e.preventDefault()));window.addEventListener("popstate",handleRouteChange);document.querySelectorAll("header nav a").forEach((link=>{link.addEventListener("click",(event=>{event.preventDefault();const route=this.getAttribute("data-route");history.pushState(null,"",`/${route}`);navigate(route)}))}));handleRouteChange();const canvas=document.getElementById("starCanvas");const starfield=new Starfield(canvas);window.addEventListener("resize",(()=>{starfield.resizeCanvas();starfield.createStars()}))}));